#include <Optional.cay>

using std::Optional;

public class DebugTest2 {
    public static void main() {
        Optional<int> opt = Optional.of(42);
        int val = opt.orElse(0);
        println("val = " + String.valueOf(val));
    }
}

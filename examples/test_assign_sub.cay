public class TestAssignSub {
    public static void main() {
        println("=== Assign Subtract Tests ===");
        int a = 20;
        a -= 5;
        print("a after -= 5: ");
        println(a);
        int b = 30;
        b -= 10;
        print("b after -= 10: ");
        println(b);
        println("All assign subtract tests completed!");
    }
}

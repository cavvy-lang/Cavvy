public int main(String[] args) {
    println("Arguments count: " + String.valueOf(args.length));
    for (int i = 0; i < args.length; i++) {
        println("Arg " + String.valueOf(i)+ ": " + args[i]);
    }
    return 0;
}

public int main() {
    // 一维数组
    int[] numbers = new int[5];
    numbers[0] = 10;
    numbers[1] = 20;

    // 数组初始化
    int[] values = {1, 2, 3, 4, 5};

    // 多维数组
    int[][] matrix = new int[3][3];
    matrix[0][0] = 1;
    matrix[1][1] = 2;
    matrix[2][2] = 3;

    // 数组长度
    println("Array length: " + String.valueOf(values.length));

    // 遍历数组
    for (int i = 0; i < values.length; i++) {
        println("values[" + String.valueOf(i)+ "] = " + values[i]);
    }

    return 0;
}
